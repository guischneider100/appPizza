import { BuscaSaborSaboresPipe } from './busca-sabor-sabores.pipe';

describe('BuscaSaborSaboresPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscaSaborSaboresPipe();
    expect(pipe).toBeTruthy();
  });
});
