import { BuscaSugestaoPipe } from './busca-sugestao.pipe';

describe('BuscaSugestaoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscaSugestaoPipe();
    expect(pipe).toBeTruthy();
  });
});
