import { BuscaSaborClientePipe } from './busca-sabor-cliente.pipe';

describe('BuscaSaborClientePipe', () => {
  it('create an instance', () => {
    const pipe = new BuscaSaborClientePipe();
    expect(pipe).toBeTruthy();
  });
});
