import { BuscaSaborPipe } from './busca-sabor.pipe';

describe('BuscaSaborPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscaSaborPipe();
    expect(pipe).toBeTruthy();
  });
});
