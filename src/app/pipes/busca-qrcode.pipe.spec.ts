import { BuscaQRCodePipe } from './busca-qrcode.pipe';

describe('BuscaQRCodePipe', () => {
  it('create an instance', () => {
    const pipe = new BuscaQRCodePipe();
    expect(pipe).toBeTruthy();
  });
});
